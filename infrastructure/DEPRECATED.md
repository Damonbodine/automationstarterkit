# Deprecated Infrastructure Artifacts

- `infrastructure/main.py`: Legacy FastAPI OCR service. Replaced by in-app OCR via Google Cloud Vision in `src/lib/ocr/pdf-ocr.ts` and queue workers. Retained for reference only.

