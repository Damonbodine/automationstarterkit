-- Example utility migration (DO NOT RUN BY DEFAULT)
-- Clears Google tokens for a single user by email. Replace placeholder below.

-- UPDATE users
-- SET
--   google_access_token = NULL,
--   google_refresh_token = NULL
-- WHERE
--   email = 'user@example.com';
